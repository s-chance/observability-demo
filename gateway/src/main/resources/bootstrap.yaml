spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    consul:
      enabled: true
      host: localhost
      port: 8500
      config:
        prefixes:
          - config
        profile-separator: "-"
        data-key: data.yaml
        format: yaml